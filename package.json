{
  "name": "heluo-code",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.24.0",
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "dev": "pnpm --filter @heluo-code/cli dev",
    "dev:desktop": "pnpm --filter @heluo-code/desktop dev",
    "build:desktop": "pnpm --filter @heluo-code/desktop build",
    "test": "vitest run",
    "test:e2e": "pnpm build:desktop && pnpm --filter @heluo-code/desktop test:e2e",
    "typecheck": "tsc -p packages/core/tsconfig.json --noEmit && tsc -p packages/cli/tsconfig.json --noEmit && tsc -p packages/plugin-web-fetch/tsconfig.json --noEmit && tsc -p packages/desktop/tsconfig.node.json --noEmit && tsc -p packages/desktop/tsconfig.web.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.6.0",
    "vitest": "^4.1.11"
  }
}
